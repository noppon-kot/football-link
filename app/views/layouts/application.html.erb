<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Football Link" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div style="max-width:960px;margin:0 auto;">
      <div style="padding:6px 12px;font-size:11px;background:#eceff1;color:#455a64;display:flex;justify-content:space-between;align-items:center;gap:8px;">
        <div style="display:flex;align-items:center;gap:8px;">
          <%= link_to "หน้าแรก", root_path, style: "text-decoration:none;color:#1565c0;font-weight:bold;" %>
          <% if can_create_tournaments? %>
            <%= link_to "แดชบอร์ด", dashboard_path, style: "text-decoration:none;color:#1565c0;" %>
          <% end %>
        </div>
        <div style="display:flex;align-items:center;gap:8px;">
          <% if current_user %>
            <span><strong><%= current_user.name %></strong></span>
            <%= button_to "ออกจากระบบ", logout_path, method: :delete, form: { style: "margin:0;display:inline;" }, class: "link-button", params: {} %>
          <% else %>
            <%= link_to "เข้าสู่ระบบ", login_path, class: "link-button" %>
          <% end %>
        </div>
      </div>

      <%= yield %>
    </div>
  </body>
</html>
