<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Ball Dernsai" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <!-- Bootstrap 5 CSS via CDN -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <!-- Bootstrap 5 JS bundle (for navbar toggle) -->
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
      crossorigin="anonymous"></script>
  </head>

  <body>
    <div style="max-width:960px;margin:0 auto;">
      <nav class="navbar navbar-expand-md navbar-light" style="background:#eceff1;border-bottom:1px solid #cfd8dc;position:sticky;top:0;z-index:1030;">
        <div class="container-fluid" style="padding:4px 12px;">
          <%= link_to root_path, class: "navbar-brand", style: "font-size:13px;color:#1565c0;font-weight:bold;display:flex;align-items:center;gap:4px;" do %>
            <%= image_tag "logo.png", size: "32x42", alt: "Ball Dernsai", style: "border-radius:4px;" %>
            <span>Dernsai Match</span>
          <% end %>

          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#topNav" aria-controls="topNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>

          <div class="collapse navbar-collapse" id="topNav">
            <ul class="navbar-nav me-auto mb-2 mb-md-0" style="font-size:12px;">
              <% if can_create_tournaments? %>
              <% end %>
            </ul>

            <ul class="navbar-nav ms-auto mb-2 mb-md-0" style="font-size:12px;align-items:center;gap:4px;">
              <% if current_user %>
                <li class="nav-item">
                  <span class="nav-link" style="color:#455a64;font-weight:bold;padding-right:4px;"> <%= current_user.name %> </span>
                </li>
                <li class="nav-item">
                  <%= button_to "ออกจากระบบ", logout_path,
                        method: :delete,
                        form: { style: "margin:0;display:inline;" },
                        class: "btn btn-sm",
                        params: {},
                        style: "background:#e3f2fd;color:#1565c0;border-color:#90caf9;" %>
                </li>
              <% else %>
                <li class="nav-item">
                  <%= link_to "เข้าสู่ระบบ", login_path, class: "btn btn-sm btn-outline-primary" %>
                </li>
                <li class="nav-item">
                  <%= link_to "เข้าสู่ระบบด้วย LINE", line_login_path, class: "btn btn-sm btn-success" %>
                </li>
              <% end %>
            </ul>
          </div>
        </div>
      </nav>

      <% flash.each do |key, message| %>
        <% next if message.blank? %>
        <% bs_class = key.to_s == "notice" ? "alert-success" : "alert-danger" %>
        <div class="container mt-2">
          <div class="alert <%= bs_class %> py-2 px-3 mb-2" role="alert" style="font-size:13px;">
            <%= message %>
          </div>
        </div>
      <% end %>

      <%= yield %>
    </div>
  </body>
</html>
